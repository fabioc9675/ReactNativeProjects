const express = require("express");
const app = express(); // server

// Settings

// Middlewares

// Routes

// Static files

// Starting the server
app.listen(3000, () => {
    console.log('server on port 3000');
})

